searchModule = angular.module('searchModule', ["ngResource"])

searchModule.config ['$httpProvider', ($httpProvider) ->
  token = $("meta[name='csrf-token']").attr 'content'
  $httpProvider.defaults.headers['common']['X-CSRF-Token'] = token
]

